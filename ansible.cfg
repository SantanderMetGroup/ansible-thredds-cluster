[defaults]
# Comment the line below if you are working on Windows
inventory = hosts

# Uncomment the line below if you are not working on Windows
#inventory = C:\cygwin64\home\syb93436\ansible-opendap-cluster\.vagrant\provisioners\ansible\inventory\vagrant_ansible_inventory

roles_path = roles
remote_user = vagrant

[ssh_connection]
ssh_args = -o ControlMaster=no