version: '3'
services:
  ansible:
    build: 
      context: .
    depends_on:
      - gateway
      - tds
  gateway:
    build:
      context: tests/
    ports:
      - "4000:8080"
  tds:
    build:
      context: tests/
