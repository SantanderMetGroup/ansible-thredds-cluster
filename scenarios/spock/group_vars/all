---
collections:
  - path: udg-tds
    top: False # not mandatory here, static catalog.html not used for ESGF scenario
    catalogs:
      src: TDSSPOCK/catalogs
      services:
        - base: /thredds/catalog
        - base: /thredds/fileServer
        - base: /thredds/dodsC
        - base: /thredds/ncss/grid

datasets:
  - src: "TDSSPOCK/datasets"
    dest: ''
      
tds_instances:
  - name: instance1
    shutdown: 18005
    tds_debug:
      jpda_address: "{{ ansible_eth0.ipv4.address }}:8000"
      connectors:
        - protocol: HTTP/1.1
          port: '18008'
      gateways:
        - id: spock
          host: "{{ ansible_nodename }}" # i.e wn020 and wn019
          replicas:
            - udg-tds
          properties:
            protocol: AJP/1.3
            port: '18009'
