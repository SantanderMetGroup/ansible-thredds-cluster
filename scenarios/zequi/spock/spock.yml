- hosts: proxies
  vars_files:
    - secret.yml
    - spockDeployment.yaml
  roles:
    - role: httpd_source_supervisor

- hosts: slaves
  vars_files:
    - secret.yml
    - spockDeployment.yaml
    - spockCatalogs.yaml
  roles:
    - role: tds_source_supervisor