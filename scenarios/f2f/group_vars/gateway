tomcat_base: '/usr/local'

tds:
  - name: tomcat
    gateways:
      - id: esgf
        host: "{{ ansible_eth1.ipv4.address }}"
        route: "{{ ansible_nodename }}"
        port: 8223
        replicas:
          - esg_dataroot
