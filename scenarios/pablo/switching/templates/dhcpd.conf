option domain-name-servers 8.8.8.8, 8.8.4.4;
option subnet-mask {{ dhcp_server_netmask }};
option routers {{ gateway_ip }};
subnet {{ dhcp_server_net }} netmask {{ dhcp_server_netmask }} {
    range {{ dhcp_range_min }} {{ dhcp_range_max }};
}
