- name: Deploy httpd 
  hosts: host-proxy
  vars_files: 
    - proxy-secret.yml
  vars: 
    user: gws-sysadmin
    password: gws-sysadmin
    virtualenv_name: TDS4GWS
    load_balancer_module: mod_proxy
  roles:
    - role: httpd