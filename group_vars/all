local_download: downloads
services_prefix: "{{ ansible_env.HOME }}/ansible"

supervisor_home: "{{ services_prefix }}/supervisor"
supervisor_etc: "{{ supervisor_home }}/etc"
supervisor_var: "{{ supervisor_home }}/var"
supervisor_programs: "{{ supervisor_etc }}/supervisord.d"

virtualenv_home: "{{ services_prefix }}/virtualenv"