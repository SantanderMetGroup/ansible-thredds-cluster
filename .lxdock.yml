name: tds-cluster
image: centos/6
#profiles:
#  - tdsc

containers:
  - name: gateway
    hostnames: [gateway]
  - name: server1
    hostnames: [server1]
  - name: server2
    hostnames: [server2]

provisioning:
  - type: ansible
    playbook: provision.yml
    lxd_transport: true
