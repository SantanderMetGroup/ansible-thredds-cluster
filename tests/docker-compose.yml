version: '3'
services:
  ansible:
    build: 
      context: ../
      dockerfile: tests/ansible/Dockerfile
    depends_on:
      - gateway
      - tds
  gateway:
    build:
      context: ssh/
    ports:
      - "4000:8080"
  tds:
    build:
      context: ssh/
