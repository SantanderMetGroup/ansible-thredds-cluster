dist: trusty
language: bash

before_install:
  - sudo apt-get update
  - sudo apt-get install -y pip vagrant

install:
  - sudo pip install ansible>=2.5
      
script:
  - cd $TRAVIS_BUILD_DIR/scenarios/devel/tests; bash test.sh
