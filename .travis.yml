dist: trusty
language: bash
sudo: required

services:
  - docker

before_install:
  - sudo apt-get update
  - sudo apt-get install -y python-pip virtualbox vagrant

install:
  - sudo pip install ansible>=2.5
      
script:
  - cd $TRAVIS_BUILD_DIR/scenarios/devel/tests; bash travis.sh
