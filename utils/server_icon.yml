- name: Substitute icon for server identification
  copy:
    src: "{{ ansible_hostname }}.png"
    dest: /home/vagrant/tomcat-base/instance1/webapps/thredds/threddsIcon.gif
  tags: [tds_deploy, icons]
