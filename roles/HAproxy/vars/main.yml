---
# vars file for roles/HAproxy

local_download: /home/syb93436/downloads
home: /home/vagrant
haproxy_home: /home/vagrant/haproxy
group: haproxy
user: haproxy

service: "{{ service_home }}/sbin/{{ service_name }}"
pidfile: "{{ service_home }}/var/run/{{ service_name }}.pid"
config: "{{ service_home }}/etc/{{ service_name }}.conf"
configtest: "{{ service_home }}/var/log/{{ service_name }}_configtest.log"