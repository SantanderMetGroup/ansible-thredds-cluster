---
# tasks file for iptables

- name: REDIRECT packets destined to VIP
  iptables:
    table: nat
    chain: PREROUTING
    destination: "{{ hostvars[{{ item.1.proxy }}.vip }}"
    jump: REDIRECT
  with_subelements:
    - "{{ tds_instances }}"
    - replicas
