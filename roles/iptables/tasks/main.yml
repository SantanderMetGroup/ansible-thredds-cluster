---
# tasks file for iptables

- name: REDIRECT packets destined to VIP
  iptables:
    table: nat
    chain: PREROUTING
    destination: "{{ hostvars['load-balancer']['ansible_eth1']['ipv4']['address'] }}"
    jump: REDIRECT
