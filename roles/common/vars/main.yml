---
# vars file for roles/common
local_download: downloads

virtualenv_name: virtualenv
virtualenv_version: 15.1.0
virtualenv_file_url: "https://pypi.python.org/packages/d4/0c/9840c08189e030873387a73b90ada981885010dd9aea134d6de30cd24cb8/{{ virtualenv_name }}-{{ virtualenv_version }}.tar.gz"
virtualenv_file_name: "{{ virtualenv_name }}-{{ virtualenv_version }}.tar.gz"
unarchive_virtualenv_name: "{{ virtualenv_name }}-{{ virtualenv_version }}"

work_home_name: WORK_HOME