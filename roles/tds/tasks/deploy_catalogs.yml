- name: Copy catalogs
  synchronize:
    src: "{{ item.1.src }}/"
    dest: "{{ tomcat_instances_path }}/{{ instance.name }}/content/thredds/{{ item.0.path }}"
    archive: False
    recursive: True
  with_subelements:
    - "{{ deployment.collections }}"
    - catalogs
