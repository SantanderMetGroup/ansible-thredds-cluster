- name: Copy catalogs
  synchronize:
    src: "{{ item.catalogs.src }}/"
    dest: "{{ tomcat_instances_path }}/{{ instance.name }}/content/thredds/{{ item.path }}"
    archive: False
    recursive: True
  with_items: "{{ collections }}"
  tags: update_catalogs
