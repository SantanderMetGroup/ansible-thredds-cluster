- name: Update proxy configuration
  shell: ssh gws-sysadmin@192.168.3.2 addWorker --gwsName={{  item.1.name }} --workerName={{ item.0.name }} --workerHost={{ item.0.ip_internal_address | default(item.0.ip_address)}} --workerPort={{ item.0.ajp.port }}
  with_subelements:
    - "{{ tds_instances }}"
    - catalog