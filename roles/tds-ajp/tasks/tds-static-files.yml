- copy:
    src: "{{ item }}"
    dest: "{{ hostvars[replica.1.id].httpd_htdocs }}/thredds/{{ item | basename }}"
  delegate_to: "{{ replica.1.id }}"
  with_fileglob: v{{ tds_major_version|string }}/*
