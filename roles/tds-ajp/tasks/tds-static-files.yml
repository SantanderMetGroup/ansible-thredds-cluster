- copy:
    src: "{{ item }}"
    dest: "{{ hostvars[replica.1.proxy].httpd_htdocs }}/thredds/{{ item | basename }}"
  delegate_to: "{{ replica.1.proxy }}"
  with_fileglob: v{{ tds_major_version|string }}/*
