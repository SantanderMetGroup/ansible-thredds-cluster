- name: Create downloads directory
  file: dest={{ downloads }} state=directory

- name: Create {{ tomcat_home }}
  file: dest={{ tomcat_home }} state=directory

- name: Download tomcat
  become: False
  run_once: True
  local_action: get_url
  args:
    url: "{{ tomcat_download_url }}"
    dest: "{{ downloads }}/{{ tomcat_filename }}"
    headers: ""

- name: Unarchive tomcat
  unarchive:
    src: "{{ downloads }}/{{ tomcat_filename }}"
    dest: "{{ tomcat_home }}"
    extra_opts:
      --strip-components=1
