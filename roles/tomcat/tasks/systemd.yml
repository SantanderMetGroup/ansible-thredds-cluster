- name: Replace tomcat@.service
  become: True
  template:
    src: tomcat.service.j2
    dest: /etc/systemd/system/multi-user.target.wants/tomcat@{{ item.name }}.service
  with_items: "{{ tomcats }}"

- name: Reload systemd daemon
  become: True
  command: systemctl daemon-reload
