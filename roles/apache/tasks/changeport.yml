- name: apache2 listen on port {{ httpd_port }}
  lineinfile: 
    dest: "{{ httpd_path }}/conf/httpd.conf" 
    regexp: "^Listen" 
    line: "Listen {{ httpd_port }}"
    state: present
  notify:
    - restart apache2