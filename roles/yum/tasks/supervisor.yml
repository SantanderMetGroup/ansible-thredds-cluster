- name: Include supervisord variables
  include_vars: file="{{ role_path }}/defaults/supervisor.yml"
  tags: always

- name: Install supervisor
  become: True
  package:
    state: present
    name: supervisor
