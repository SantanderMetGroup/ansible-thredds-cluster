httpd_htdocs: /var/www/html
httpd_conf: "{{ '/etc/httpd/conf' if ansible_pkg_mgr == 'yum' else '/etc/apache2' }}"
httpd_conf_file: "{{ httpd_conf ~ '/httpd.conf' if ansible_pkg_mgr == 'yum' else httpd_conf ~ '/apache2.conf' }}"
httpd_pid: "{{ httpd_root }}/run/httpd.pid"
